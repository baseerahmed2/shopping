import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import ProductDetailScreen from './HomeDetailScreen'

const index = props => {
    return (
       <ProductDetailScreen {...props} />
    )
}

export default index

